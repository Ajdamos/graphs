

export const GraphMenu = (props) => {
    const {
        setMode,
        pathPoints,
        setPathPoints, 
        } = props

    return (
        <div className='flex flex-col items-center'>
        </div>

    )
}